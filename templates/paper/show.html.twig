{% extends 'base.html.twig' %}

{% block title %}Paper{% endblock %}

{% block body %}
    <h1>Paper</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paper.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ paper.name }}</td>
            </tr>
            <tr>
                <th>Width</th>
                <td>{{ paper.width }}</td>
            </tr>
            <tr>
                <th>Height</th>
                <td>{{ paper.height }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ paper.weight }}</td>
            </tr>
            <tr>
                <th>Price_per_sheet</th>
                <td>{{ paper.pricePerSheet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paper_index') }}">back to list</a>

    <a href="{{ path('app_paper_edit', {'id': paper.id}) }}">edit</a>

    {{ include('paper/_delete_form.html.twig') }}
{% endblock %}
