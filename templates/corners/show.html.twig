{% extends 'base.html.twig' %}

{% block title %}Corners{% endblock %}

{% block body %}
    <h1>Corners</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ corner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ corner.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ corner.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_corners_index') }}">back to list</a>

    <a href="{{ path('app_corners_edit', {'id': corner.id}) }}">edit</a>

    {{ include('corners/_delete_form.html.twig') }}
{% endblock %}
