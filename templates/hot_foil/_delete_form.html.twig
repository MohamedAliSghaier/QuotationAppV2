<form method="post" action="{{ path('app_hot_foil_delete', {'id': hot_foil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hot_foil.id) }}">
    <button class="btn">Delete</button>
</form>
