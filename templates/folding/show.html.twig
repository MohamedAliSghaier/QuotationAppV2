{% extends 'base.html.twig' %}

{% block title %}Folding{% endblock %}

{% block body %}
    <h1>Folding</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ folding.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ folding.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_folding_index') }}">back to list</a>

    <a href="{{ path('app_folding_edit', {'id': folding.id}) }}">edit</a>

    {{ include('folding/_delete_form.html.twig') }}
{% endblock %}
