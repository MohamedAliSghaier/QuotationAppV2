<form method="post" action="{{ path('app_folding_delete', {'id': folding.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ folding.id) }}">
    <button class="btn">Delete</button>
</form>
